Create a new copy here!

<%= form_for [@book, @copy] do |f| %>
  <%= f.label(:book_id) %>
  <%= f.text_field(:book_id) %>
  <%= f.label(:due_date) %>
  <%= f.text_field(:due_date) %>
  <%= submit_tag("Submit") %>
<% end %>
