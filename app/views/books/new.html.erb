Create a new book here!

<%= form_for(Book.new) do |f| %>
  <%= f.label(:title) %>
  <%= f.text_field(:title) %>
  <%= f.label(:author) %>
  <%= f.text_field(:author) %>
  <%= f.label(:isbn) %>
  <%= f.text_field(:isbn) %>
  <input value="Send" type="submit"/>
<% end %>